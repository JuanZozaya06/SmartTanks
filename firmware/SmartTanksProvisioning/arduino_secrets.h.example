#pragma once

// Archivo para preaprovisionar en fábrica o en el instalador autorizado.
// Nunca se entrega ni se muestra al usuario final, y nunca se confirma en Git.
// El secreto corresponde al deviceId de esta unidad y ya existe hasheado en
// Firestore con estado "unclaimed" antes de que el cliente la reciba.
#define SMARTTANKS_DEVICE_SECRET "replace-with-the-secret-for-this-unit"
